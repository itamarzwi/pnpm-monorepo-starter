{
  "name": "@org/validate-request",
  "description": "Library for validating input in requests for express routes",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "rimraf dist && ci-change-entry-point && tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit && eslint \"./**/*.ts\" --max-warnings 0",
    "lint:fix": "eslint \"./**/*.ts\" --max-warnings 0 --fix",
    "test": "node --import=tsx --test src/index.test.ts && tsc --noEmit"
  },
  "dependencies": {
    "@org/errors": "workspace:*"
  },
  "devDependencies": {
    "@org/ci-change-entry-point": "workspace:*",
    "@tsconfig/node22": "catalog:",
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "zod": "catalog:"
  }
}
